mutation {
  createSubscription(input:{userId: <%= user_id %>, plan: <%= plan %> }) {
    subscription {
      id
      status
      end_date
      prices {
        currency
        value
      }
    }
  }
}
