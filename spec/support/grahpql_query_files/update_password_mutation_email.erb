mutation {
  updateUserPassword(input:{pinNumber: "<%= pin_number %>", email: "<%= email %>", oldPassword: "<%=old_password %>", newPassword: "<%=new_password %>"}){
    user {
      displayName
      password
    }
  }
}
