mutation {
  updateUserPassword(input:{pinNumber: "<%= pin_number %>", phoneNumber: "<%= phone_number %>", oldPassword: "<%=old_password %>", newPassword: "<%=new_password %>"}){
    user {
      displayName
      password
    }
  }
}
