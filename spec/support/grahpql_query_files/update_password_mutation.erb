mutation {
  updateUserPassword(input:{
    email: "<%= email %>",
    pinNumber: "<%= pin_number %>",
    newPassword: "<%= new_password %>"
  }) {
    user {
      id
      displayName
      password
    }
  }
}
